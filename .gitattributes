# Shell scripts must use LF line endings (Unix) to run on Unix systems
*.sh text eol=lf

# Batch files use CRLF line endings (Windows)
*.bat text eol=crlf

# Use bd merge for beads JSONL files
.beads/issues.jsonl merge=beads
