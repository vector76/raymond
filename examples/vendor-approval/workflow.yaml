id: vendor-approval
name: Vendor Approval Workflow
description: >
  Evaluates a vendor against company policy, pauses for human approval,
  and generates a recommendation report.
input_schema:
  vendor_name: string
  budget_limit: string
default_budget: 5.0
requires_human_input: auto
