Your task is to call a sub-workflow at an explicit entry point and wait for its result.

Respond with exactly this (nothing else):
<call-workflow return="2_DONE.md">../cross_workflow_entry_sub/OTHER_ENTRY.md</call-workflow>
